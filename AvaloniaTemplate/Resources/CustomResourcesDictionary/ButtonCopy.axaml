<ResourceDictionary xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="using:AvaloniaTemplate.Resources.CustomResourcesDictionary"
        >

    <Design.PreviewWith>
        <Border Padding="20">
            <controls:ButtonCopy />
        </Border>
    </Design.PreviewWith>

  <ControlTheme x:Key="{x:Type controls:ButtonCopy}" TargetType="controls:ButtonCopy">
    <Setter Property="FontFamily" Value="Verdana"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Template">
            <ControlTemplate>
                <Button Padding="5 2 5 2"
                        Classes="disableBackgroundTransparent"
                        Background="{TemplateBinding Background}"
                        Command="{TemplateBinding Command}"
                        CommandParameter="{TemplateBinding CommandParameter}"
                        >
                    <Button.Content>
                        <Grid ColumnDefinitions="* auto">
                            <Image Source="\Assets\Copy.png" Height="20"/>
                            <TextBlock Grid.Column="1"
                                   Text="Копирвоать"
                                   Margin="5 0 0 0"
                                   VerticalAlignment="Center"
                                   FontFamily="{TemplateBinding FontFamily}"
                                   FontSize="{TemplateBinding FontSize}"
                                   />

                        </Grid>
                    </Button.Content>
                </Button>
            </ControlTemplate>
        </Setter>
  </ControlTheme>
</ResourceDictionary>
