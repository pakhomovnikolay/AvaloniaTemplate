<ResourceDictionary xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="using:AvaloniaTemplate.Resources.CustomResourcesDictionary"
        >
    
    <Design.PreviewWith>
        <Border Padding="20">
            <controls:ButtonSimpleAs />
        </Border>
    </Design.PreviewWith>
    
    <ControlTheme x:Key="{x:Type controls:ButtonSimpleAs}"
                  TargetType="controls:ButtonSimpleAs"
                  >
        <Setter Property="FontFamily" Value="Verdana"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <ControlTemplate>
                <Button Padding="5 2 5 2"
                        Classes="disableBackgroundTransparent"
                        Background="{TemplateBinding Background}"
                        Command="{TemplateBinding Command}"
                        CommandParameter="{TemplateBinding CommandParameter}"
                        >
                    <Button.Content>
                        <Grid ColumnDefinitions="* auto">
                            <Image Source="\Assets\AsSimple.png" Height="20"/>
                            <TextBlock Grid.Column="1"
                                   Text="Фомат по образцу"
                                   Margin="5 0 0 0"
                                   VerticalAlignment="Center"
                                   FontFamily="{TemplateBinding FontFamily}"
                                   FontSize="{TemplateBinding FontSize}"
                                   />

                        </Grid>
                    </Button.Content>
                </Button>
            </ControlTemplate>
        </Setter>
    </ControlTheme>
</ResourceDictionary>
